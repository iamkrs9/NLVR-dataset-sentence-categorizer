{'at the top': ['there is exactly one tower with a black block at the top', 'there is exactly one tower with a blue block at the top', 'there is exactly one tower with a yellow block at the top', 'there is exactly one tower with a blue block at the base and yellow block at the top'],

'not touching': ['there is exactly one black circle not touching the edge', 'there is exactly on black circle not touching any edge', 'there is exactly one black square not touching the edge', 'there is exactly one yellow triangle not touching the edge', 'there is exactly one black square not touching any edge', 'there is exactly one blue circle not touching any edge', 'there is exactly one blue triangle not touching the edge', 'there is exactly one yellow circle not touching any edge', 'there is exactly one yellow object not touching any edge', 'there is exactly one black triangle not touching any edge', 'there is exactly one yellow square not touching any edge', 'there is exactly one black triangle not touching the edge', 'there is exactly one blue triangle not touching any edge', 'there is exactly one circle not touching any edge', 'there is exactly one black circle not touching any edge', 'there is exactly one blue circle not touching any adge'],

'touching': ['there is exactly one yellow square touching the edge', 'there is exactly one triangle closely touching the bottom of a box.', 'there is exactly one black circle touching the edge', 'there is exactly one yellow object touching the edge', 'there is exactly one triangle touching the base', 'there is exactly one blue item closely touching right wall of a box.', 'there is exactly one black object touching the edge', 'there is exactly one blue object touching the edge', 'there is exactly one blue square touching the base', 'there is exactly one yellow square closely touching the bottom of a box.', 'there is exactly one black object touching the base', 'there is exactly one yellow triangle touching the base', 'there is exactly one blue object touching an edge', 'there is exactly one blue circle touching an edge', 'there is exactly one circle closely touching right wall of a box.', 'there is exactly one blue object touching the base', 'there is exactly one yellow triangle touching the edge', 'there is exactly one black square touching the edge', 'there is exactly one yellow object touching any edge', 'there is exactly one black item closely touching the bottom of a box.', 'there is exactly one black circle touching the base', 'there is exactly one circle touching the edge', 'there is exactly one  square touching the bottom base', 'there is exactly one yellow object is touching an edge', 'there is exactly one square touching the base', 'there is exactly one yellow object touching the base', 'there is exactly one blue triangle touching the bottom base', 'there is exactly one square closely touching right wall of a box.', 'there is exactly one black square touching the base', 'there is exactly one blue item closely touching the bottom of a box.', 'there is exactly one blue square closely touching the bottom of a box.', 'there is exactly one blue square touching the wall.', 'there is exactly one blue circle touching the edge', 'there is exactly one blue circle touching any edge', 'there is exactly one black triangle touching the base', 'there is exactly one black circle touching the bottom base', 'there is exactly one yellow object touching the bottom base', 'there is exactly one square touching any edge', 'there is exactly one yellow circle touching the egde'],

'as the base of': ['there is exactly one yellow block as the base of a tower with three blocks.', 'there is exactly one black block as the base of a tower.', 'there is exactly one blue block as the base of a tower.', 'there is exactly one yellow block as the base of a tower with at least two blocks.'],

'with a': ['there is exactly one tower with a blue block at the base', 'there is exactly one tower with two blocks and with a black block at the base', 'there is exactly one tower with a blue block over a  yellow block', 'there is exactly one tower with a black block at the base', 'there is exactly one tower with a yellow block at base', 'there is exactly  one tower with a yellow block at the base', 'there is exactly one tower with a yellow block at the base', 'there is exactly one tower with a black block at base', 'there is exactly one black block as the top of a tower with at least two blocks.', 'there is exactly one tower with a black block above a yellow block', 'there is exactly one tower with a blue block at base', 'there is exactly one tower with a yellow black at the base', 'there is exactly one tower with a single object', 'there is exactly one blue block as the top of a tower with at least three blocks.', 'there is exactly one tower with a yellow block at the base.', 'there is exactly one tower with a yellow block over a blue block', 'there is exactly one tower with a black block above a blue block'],

'tower with': ['there is exactly one tower with four blocks', 'there is exactly one tower with black block over a yellow block', 'there is exactly one tower with only black blocks'],

'as the top': ['there is exactly one yellow block as the top of a tower.', 'there is exactly one black block as the top of a tower.', 'there is exactly one blue block as the top of a tower.'],

'1 blue circle': ['there is exactly 1 blue circle'],

'tower which has': ['there is exactly one tower which has a blue block over a yellow block', 'there is exactly one  tower which has a black block at the base', 'there is exactly one tower which has a black block at the base', 'there is exactly one tower which has a blue block over a black block'],

'with one item': ['there is exactly one box with one item.'],

'block on a black block': ['there is exactly one yellow block on a black block.'],

'1 black item': ['there is exactly 1 black item'],

'1 yellow item': ['there is exactly 1 yellow item'],

'square and': ['there is exactly 1 yellow square and 1 yellow triangle']}